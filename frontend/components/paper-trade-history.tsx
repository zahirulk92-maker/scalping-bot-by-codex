import type { ClosedPaperTrade } from "@/types/bot";

export function PaperTradeHistory({ trades }: { trades: ClosedPaperTrade[] }) {
  return <section className="overflow-hidden rounded-xl border border-slate-800 bg-slate-900/70"><div className="border-b border-slate-800 p-5"><h2 className="font-semibold">PAPER TRADES</h2><p className="mt-1 text-xs text-amber-300">SIMULATED ONLY — NO REAL ORDERS</p></div>{trades.length === 0 ? <p className="p-5 text-sm text-slate-400">No closed paper trades yet.</p> : <div className="overflow-x-auto"><table className="min-w-full text-left text-xs"><thead className="bg-slate-950/60 text-slate-500"><tr>{["Time", "Symbol", "Direction", "Entry", "Exit", "Reason", "Gross", "Fees", "Net"].map((label) => <th className="px-3 py-3 font-medium" key={label}>{label}</th>)}</tr></thead><tbody>{trades.map((trade) => <tr className="border-t border-slate-800" key={trade.trade_id}><td className="px-3 py-3 text-slate-400">{trade.closed_at}</td><td className="px-3 py-3">{trade.symbol}</td><td className="px-3 py-3 uppercase">{trade.direction}</td><td className="px-3 py-3">{trade.entry_fill_price}</td><td className="px-3 py-3">{trade.exit_fill_price}</td><td className="px-3 py-3 uppercase">{trade.exit_reason.replace("_", " ")}</td><td className="px-3 py-3">{trade.gross_pnl_usdt}</td><td className="px-3 py-3">{trade.fees_usdt}</td><td className={`px-3 py-3 font-semibold ${Number(trade.net_pnl_usdt) >= 0 ? "text-emerald-300" : "text-rose-300"}`}>{trade.net_pnl_usdt}</td></tr>)}</tbody></table></div>}</section>;
}
